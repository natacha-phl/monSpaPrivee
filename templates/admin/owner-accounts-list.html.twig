{% extends 'base.html.twig' %}

{% block body %}

    <h1>Liste des comptes owner</h1>

    <table class="table">
        <tr>
            <th>Prénom</th>
            <th>Nom</th>
            <th>Email</th>
            <th></th>
        </tr>


        <tbody>
        {% for owner in owners %}
            <tr>
                <td>{{ owner.firstName}}</td>
                <td>{{ owner.lastName}}</td>
                <td>{{ owner.email }}</td>
                <td> <a href="#"> > </a> </td>


            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}

