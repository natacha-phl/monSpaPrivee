{% extends 'base.html.twig' %}

{% block body %}
    <section class="h-100 h-custom">
        <div class="container py-5 h-100">
            <div class="row d-flex justify-content-center align-items-center h-100">
                <div class="col">
                    <div class="card">
                        <div class="card-body p-4">
                            <div class="row">
                                <div class="col-lg-7">
                                    <div class="d-flex justify-content-between align-items-center mb-4">
                                        <div>
                                            <p class="mb-1">Panier</p>
                                            <p class="mb-0">Vous avez {{ items | length }} élement(s) dans votre
                                                panier</p>
                                        </div>
                                    </div>
                                    {% for item in items %}
                                        <div class="card mb-3">
                                            <div class="card-body">
                                                <div class="d-flex justify-content-between">
                                                    <div class="d-flex flex-row align-items-center">
                                                        <div>
                                                            <img
                                                                    src="https://mdbcdn.b-cdn.net/img/Photos/new-templates/bootstrap-shopping-carts/img1.webp"
                                                                    class="img-fluid rounded-3" alt="Shopping item"
                                                                    style="width: 65px;">
                                                        </div>
                                                        <div class="ms-3">
                                                            <h5>{{ item.room.name }}</h5>
                                                            <p class="small mb-0">
                                                                {{ item.room.priceHour }}
                                                                {{ item.bookingHours }} &euro;

                                                            </p>
                                                        </div>
                                                    </div>
                                                    <div class="d-flex flex-row align-items-center">
                                                        <div style="width: 50px;">
                                                        1 QUANTITE
                                                        </div>
                                                        <div style="width: 80px;">
                                                            <h5 class="mb-0">{{ (item.room.priceHour * item.bookingHours) }}
                                                                &euro;</h5>
                                                        </div>
                                                        <a href="#!" style="color: #cecece;"><i
                                                                    class="fas fa-trash-alt"></i></a>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    {% endfor %}
                                </div>

                                <div class="col-lg-5">

                                    <div class="card bg-body-tertiary text-dark rounded-3">
                                        <div class="card-body">

                                            <div class="d-flex justify-content-between">
                                                <p class="mb-2">Total</p>
                                                <p class="mb-2">{{ total }} &euro;</p>
                                            </div>

                                            <a href="{{ path('payment_stripe') }}"
                                               class="d-flex justify-content-center btn btn-info btn-block btn-lg">
                                                <span>Payer&nbsp;</span>
                                                <span>{{ total }}&euro;</span>
                                            </a>

                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}



{#
{% block body %}

    <h1>Page mon panier</h1>

    {% if items | length > 0 %}
        <table class="table">
            <tr>
                <th>Produit</th>
                <th>Prix</th>
                <th>Durée</th>
                <th>Sous-total</th>
                <th></th>
            </tr>


            <tbody>
            {% for item in items %}
                <tr>
                    <td>{{ item.room.name }}</td>
                    <td>{{ item.room.priceHour }}<span>€</span></td>
                    <td>{{ item.bookingHours }}</td>
                    <td>{{ (item.room.priceHour * item.bookingHours) }}<span>€</span></td>
                    <td>
                        <form action="{{ path('cart_remove') }}" method="GET">
#}
{#                            <select name="itemLineNumber"  id="delete-select"> #}{#
<input type="hidden" name="itemLineNumber" value="{{item.lineNumber}}">
#}
{#                                <option value="{{item.lineNumber}}">supprimer</option> #}{#
#}
{#                            </select> #}{#
<button type="submit" class="btn btn-danger btn-sm"
>x
</button>
</form>
#}
{#                        <a href="{{ path('cart_remove', {'id': item.room.id}) }}" class="btn btn-danger btn-sm"> <i #}{#
#}
{#                                    class="fas fa-trash"></i></a> #}{#
</td>
</tr>
{% endfor %}
</tbody>
<tfoot>
<tr>
<td colspan="3" class="text-right">Total :
</td>
<td>{{ total }}<span>€</span></td>
<td></td>
</tr>
</tfoot>
</table>

<a href="{{ path('payment_stripe') }}" class="d-flex justify-content-center"><button id="checkout-button" class="btn btn-secondary btn-md button btn-cart">Payer</button></a>
{% else %}
<p>Votre panier est vide !</p>

{% endif %}

{% endblock %} #}
